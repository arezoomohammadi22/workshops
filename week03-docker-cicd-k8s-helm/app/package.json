{
  "name": "web-app",
  
  "version": "1.0.0",
  "description": "A sample Node.js app for Kubernetes deployment using Helm and GitLab CI/CD.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
